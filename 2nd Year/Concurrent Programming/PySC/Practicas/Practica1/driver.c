/*#include "gestion_memoria.h"
#include <stdio.h>
#include <stdlib.h>


int main(){
	T_Manejador m;
	unsigned dir,dir2,ok,tam,tam2;
	tam=200;
	tam2=500;
	crear(&m);
	mostrar(m);
	obtener(&m,tam,&dir,&ok);
	mostrar(m);
	obtener(&m,tam2,&dir2,&ok);
	mostrar(m);
	devolver(&m,tam,dir);
	mostrar(m);

	return 0;
}*/
